# responds with data from the query string
- :conditions:
    body:
      email:
        !save nick: !regexp /(\w)@.*/i
      # email: !save nick
      # email: !save username: !!js/regexp /(.*)@.*/
  :response:
    output: !get nick

- :condtions:
    params.has: getter
  :response:
    output:
      !get nick: !random name
# !get nick
# !get nick: default
