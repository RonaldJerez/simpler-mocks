# responds with data from the query string
- :conditions:
    body:
      email: !save { nick: !!js/function (val) => typeof val === 'string' }
      # email: !save nick
      # email: !save { username: !!js/regexp /(.*)@.*/ }
  :response:
    output: !get nick

- :conditions:
  :response:
    output: !get { nick: !random name }
# !get nick
# !get { nick: default }
